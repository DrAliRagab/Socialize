name: "Auto Changelog"

on: [push, release, workflow_dispatch]

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: Changelog
      uses: ardalanamini/auto-changelog@v3
      id: changelog
